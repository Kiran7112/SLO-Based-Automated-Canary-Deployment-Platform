apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Order matters only for readability; kubectl applies the set together.
resources:
  - namespace.yaml
  - service.yaml
  - ingress.yaml
  - analysistemplate.yaml
  - rollout.yaml

commonLabels:
  app.kubernetes.io/part-of: slo-canary-platform
